{
  "name": "perfect-xi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "bun src/server.ts",
    "build:data": "bun scripts/build-data.ts && bun scripts/build-europe.ts",
    "fetch:data": "bun scripts/fetch-data.ts",
    "test": "bun test",
    "test:e2e": "playwright test",
    "build:europe": "bun scripts/build-europe.ts"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/bun": "latest",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}